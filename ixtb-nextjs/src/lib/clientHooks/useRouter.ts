/**
 * App Router `useRouter` that also starts the top navigation progress bar.
 * Prefer this over `next/navigation` whenever navigating with push/replace.
 */
export { useRouter } from "nextjs-toploader/app";
